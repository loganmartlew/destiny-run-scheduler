import { handleAuth } from '@auth0/nextjs-auth0';

export default handleAuth();

/*
AUTH IS KINDA OK
just need to add functionality to create new user on creation
and link auth0 id into sqlite use id
maybe use nextauth or auth0-js for custom login page and actions
*/
